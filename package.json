{
  "name": "email-builder",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest tests/app.test.js --no-coverage",
    "test:sanitizer": "jest tests/textSanitizer.test.js --no-coverage",
    "test:all": "jest tests/ --no-coverage"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jsdom": "^24.1.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
